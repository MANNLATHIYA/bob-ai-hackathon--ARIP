name: Clinical Trial Risk Monitor & Protocol Deviation Detector
short_name: Trial Sentinel
track: Pharma & Biotech
description: Explainable protocol deviation detection, site risk ranking, and human-reviewable CAPA drafts using synthetic clinical-trial data.
repository: REPLACE_WITH_REPOSITORY_URL
demo_url: REPLACE_WITH_DEMO_URL
video_url: REPLACE_WITH_VIDEO_URL
team:
  name: REPLACE_WITH_TEAM_NAME
  members:
    - name: REPLACE_WITH_MEMBER_NAME
      role: REPLACE_WITH_ROLE
data_policy:
  synthetic_only: true
  contains_phi: false
run:
  command: docker compose up --build
  frontend: http://localhost:3000
  api_docs: http://localhost:8000/docs

