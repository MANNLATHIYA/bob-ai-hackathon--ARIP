# TrialSutra Mac launcher

The `TrialSutra.app` bundle starts the local API and compiled dashboard when opened and then opens `http://localhost:3000`. Copy the app to `~/Applications` after cloning this repository. It expects the Python environment and frontend build described in `docs/setup-guide.md`.

